#include "main.h"

/**
 *add - function that adds two integers and returns the result
 *@a ,@b: integers to check
 *Return: Always result .
 */

int add(int a, int b)
{
	int r;
	r = a + b;
	return (r);
}
