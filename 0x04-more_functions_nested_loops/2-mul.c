#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: integer to multiply.
 * @b: integer to multiply.
 * Return: result.
 */

int mul(int a, int b)
{
	int r;

	r = a * b;
	return (r);
}
